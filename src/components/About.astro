---
import Section from "./Section.astro";
import type { AboutProps, HeroProps } from "@types";

type Props = AboutProps & Pick<HeroProps, "name">;

const { description, principles = [] } = Astro.props;
---

<Section text="Skills & Practice" href="about">
  <div class="space-y-8">
    <p class="max-w-2xl text-base text-neutral">{description}</p>

    <div class="space-y-4">
      {
        principles.map((principle) => (
          <div class="rounded-2xl border border-neutral/20 bg-[#121a23] px-5 py-4">
            <p class="text-base text-neutral">{principle}</p>
          </div>
        ))
      }
    </div>
  </div>
</Section>
